"""Specialised agents that make up the AI Solution Architect pipeline.

Each module exposes a single `run(...)` function and behaves like an
independent, focused agent. The orchestrator chains them together.
"""
