# ── Streamlit app dependencies (what app.py actually needs) ───────────────
# Ranges (not exact pins) so the resolver always finds prebuilt wheels for
# whatever Python version Streamlit Cloud runs on.

streamlit>=1.39,<2

# AI / LLM — GPT-4o via LangChain. langchain >=0.3.27 dropped the numpy<2 cap
# that breaks installs on Python 3.13+.
langchain>=0.3.27,<0.4
langchain-openai>=0.2.14,<0.4
openai>=1.51

# PDF in / PDF out
pypdf>=5.0
fpdf2>=2.8

# Data models + config
pydantic>=2.9,<3
python-dotenv>=1.0

# Force a prebuilt numpy wheel (avoids the 1.26.x source build that fails on 3.13).
numpy>=2.1

# NOTE: the optional FastAPI backend (backend/main.py) is NOT needed to run the
# Streamlit app or to deploy on Streamlit Cloud. To run that backend locally,
# also install:  pip install -r requirements-backend.txt
